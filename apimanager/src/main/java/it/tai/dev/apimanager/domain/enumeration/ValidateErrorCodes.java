package it.tai.dev.apimanager.domain.enumeration;

/**
 * Error codes for resources status transition
 */
public enum ValidateErrorCodes {
    OK,
    UNAUTHORIZED,
    INVALID_TRANSITION,
    NOT_EXIST
}
